name: text_tab_bar
description: Flutter package that implements a minimalistic tabbar widget with animated text 
version: 0.0.1
homepage: https://github.com/Filastian/text_tab_bar
repository: https://github.com/Filastian/text_tab_bar

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

flutter: